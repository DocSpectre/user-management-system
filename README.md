A basic user management system API implementing authentication (login page), CRUD functions, wherein
admin can:
• Add a new user
• Edit a user
• Delete a user
• View list of all users in the system
• Allow multiple users to be removed
